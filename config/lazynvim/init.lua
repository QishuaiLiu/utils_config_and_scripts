-- bootstrap lazy.nvim, LazyVim and your plugins
-- Large files are handled per-buffer by LazyVim's built-in snacks.bigfile
-- (treesitter/LSP/etc. disabled on big buffers) instead of disabling the
-- whole config for the session.
require("config.lazy")
